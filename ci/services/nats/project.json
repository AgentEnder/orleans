{
  "name": "svc-nats",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "continuous": true,
      "cache": false,
      "options": {
        "command": "nats-server --js --port 4222 --http_port 8222"
      }
    }
  }
}
